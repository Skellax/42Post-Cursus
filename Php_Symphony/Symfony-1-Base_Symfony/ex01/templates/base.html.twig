<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        {% block stylesheets %}
        <style>
            .hover-effect {
                transition: transform 0.2s ease-in-out;
            }
            .hover-effect:hover{
                transform: scale(1.2);
                font-size: bolder;
            }

        </style>
        {% endblock %}
    </head>
    <body class = "d-flex flex-column min-vh-100">
        {% block body %}
            <header>
                <nav class="navbar fs-5 bg-primary text-white navbar-expand-lg">
                    <div class="container-fluid">
                        <a class="navbar-brand text-white font-weight-bold" href="{{ url('main')}}">E01</a>
                        <div class="collapse navbar-collapse">
                            <ul class="navbar-nav">
                                <li class = "nav-item">
                                    <a class="nav-link fs-5 mx-2 text-white hover-effect" href = "{{ url('main')}}">Accueil </a>
                                </li>
                                {% for article in articles %}
                                    <li class = "nav-item">
                                    <a class="nav-link fs-5 mx-2 text-white hover-effect" href = "{{ path('article', {'article': article})}}">{{article}} </a>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </nav>
            </header>

            <main class= "flex-grow-1">
                {% block content %}
                {% endblock %}
            </main>

            <footer class= "bg-dark text-white text-center">E01 Php symphony </footer>

        {% endblock %}
    </body>
</html>
